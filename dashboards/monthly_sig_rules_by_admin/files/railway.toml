
[build]
builder = "nixpacks"
buildCommand = "pip install -r dashboards/monthly_sig_rules/files/requirements.txt"

[deploy]
startCommand = "streamlit run dashboards/monthly_sig_rules/files/monthly_sig_rules.py --server.port $PORT --server.address 0.0.0.0 --server.headless=true"
healthcheckPath = "/_stcore/health"
restartPolicyType = "on_failure"
